import { styled } from "nativewind";
import WindImage from "./WindImage";
import WindText from "./WindText";
import WindView from "./WindView";
import { Text, TouchableNativeFeedback, TouchableOpacity, TouchableWithoutFeedback } from "react-native";
import WindFlatList from "./WindFlatList";
import WindTextInput from "./WindTextInput";

export const WinImage = WindImage;
export const WinView = WindView
export const WinText = styled(Text)
export const WinTextInput = WindTextInput
export const WinTouchableOpacity = styled(TouchableOpacity)
export const WinTouchableWithoutFeedback = styled(TouchableWithoutFeedback)
export const WinTouchableNativeFeedback = styled(TouchableNativeFeedback)
export const WinFlatList = WindFlatList
